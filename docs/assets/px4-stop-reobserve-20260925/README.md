# Stop/reobserve development attempt — 2026-09-25

**The comparison is incomplete.** One of the two planned sessions was attempted.
It reached the planned stop, but the origin-only camera capture guard refused
the new observation at that location. Recovery landed/disarmed the vehicle and
removed the simulator. No resume route was executed; no navigation improvement
is claimed.

The [reviewed attempt record](development-attempt-01.json) retains counts,
runtime-source and raw-record hashes without keys, approval records, private
paths or task databases. It is an observed failed development attempt, not the
completed comparison. The next pair must be frozen separately after the narrow
checkpoint-capture correction and storage admission.

See the [protocol and current validation status](../../agents/px4-stop-reobserve-trial.md).
The replay exporter exists, but no paired video/replay has been produced or
validated yet. Original raw records are retained locally in the task's ignored
output directory.
